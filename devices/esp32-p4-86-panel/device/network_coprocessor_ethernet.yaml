# =============================================================================
# NETWORK CO-PROCESSOR - Ethernet firmware variant
# =============================================================================

substitutions:
  hosted_wifi_coprocessor: "disabled"

ethernet:
  type: IP101
  mdc_pin: GPIO31
  mdio_pin: GPIO52
  clk_pin: GPIO50
  phy_addr: 1
  power_pin: GPIO51
